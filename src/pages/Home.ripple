import type { Theme } from '../lib/theme';
import { Icon } from '../components/Icon.ripple';
import { skills } from '../data/skills';
import { projects } from '../data/projects';

export component Home(props: { theme: Theme }) {
  <div class="home">
    <section id="projects" class="section">
      <h2 class="section-title">{"Projects"}</h2>
      <div class="projects-grid">
        for (const project of projects) {
          <a 
            href={project.url || '#'} 
            class="project-card" 
            target={project.url ? '_blank' : undefined}
            rel={project.url ? 'noopener noreferrer' : undefined}
          >
            <h3 class="project-title">{project.name}</h3>
            <p class="project-description">{project.description}</p>
            <div class="project-tech">
              for (const tech of project.tech) {
                <span class="tech-tag">{tech}</span>
              }
            </div>
          </a>
        }
      </div>
    </section>

    <section id="experience" class="section">
      <h2 class="section-title">{"Experience"}</h2>
      <div class="experience-list">
        <div class="experience-item">
          <div class="experience-header">
            <h3 class="experience-company">{"Techverito Software Solutions"}</h3>
            <div class="experience-period">{"Aug 2022 - Present"}</div>
          </div>
          <p class="experience-role">{"Led the development team in creating robust test automation frameworks and improving software quality processes."}</p>
        </div>
        <div class="experience-item">
          <div class="experience-header">
            <h3 class="experience-company">{"Medline (Contractor)"}</h3>
            <div class="experience-period">{"Feb 2024 - Jun 2025"}</div>
          </div>
          <p class="experience-role">{"Developed and implemented quality assurance strategies for enterprise-level applications, ensuring high-quality software delivery."}</p>
        </div>
        <div class="experience-item">
          <div class="experience-header">
            <h3 class="experience-company">{"Freelance Projects"}</h3>
            <div class="experience-period">{"2021 - 2022"}</div>
          </div>
          <p class="experience-role">{"Designed and developed web applications for various clients, focusing on performance and user experience."}</p>
        </div>
      </div>
    </section>

    <section id="tools" class="section">
      <h2 class="section-title">{"Tools"}</h2>
      <div class="skills-categories">
        <div class="skill-category">
          <h3 class="category-title">{"Programming Languages"}</h3>
          <div class="tools-grid">
            <div class="tool-card"><Icon name="java" size={20} /><span>{"Java"}</span></div>
            <div class="tool-card"><Icon name="javascript" size={20} /><span>{"JS"}</span></div>
            <div class="tool-card"><Icon name="typescript" size={20} /><span>{"TS"}</span></div>
            <div class="tool-card"><Icon name="python" size={20} /><span>{"Python"}</span></div>
          </div>
        </div>
        <div class="skill-category">
          <h3 class="category-title">{"Libraries"}</h3>
          <div class="tools-grid">
            <div class="tool-card"><Icon name="react" size={20} /><span>{"React"}</span></div>
            <div class="tool-card"><Icon name="selenium" size={20} /><span>{"Selenium"}</span></div>
            <div class="tool-card"><Icon name="rest-assured" size={20} /><span>{"REST"}</span></div>
          </div>
        </div>
        <div class="skill-category">
          <h3 class="category-title">{"Tools"}</h3>
          <div class="tools-grid">
            <div class="tool-card"><Icon name="aws" size={20} /><span>{"AWS"}</span></div>
            <div class="tool-card"><Icon name="terraform" size={20} /><span>{"Terraform"}</span></div>
            <div class="tool-card"><Icon name="git" size={20} /><span>{"Git"}</span></div>
            <div class="tool-card"><Icon name="jenkins" size={20} /><span>{"Jenkins"}</span></div>
            <div class="tool-card"><Icon name="docker" size={20} /><span>{"Docker"}</span></div>
            <div class="tool-card"><Icon name="postman" size={20} /><span>{"Postman"}</span></div>
            <div class="tool-card"><Icon name="mongodb" size={20} /><span>{"MongoDB"}</span></div>
            <div class="tool-card"><Icon name="nodejs" size={20} /><span>{"Node.js"}</span></div>
          </div>
        </div>
        <div class="skill-category">
          <h3 class="category-title">{"Frameworks"}</h3>
          <div class="tools-grid">
            <div class="tool-card"><Icon name="langchain" size={20} /><span>{"LangChain"}</span></div>
            <div class="tool-card"><Icon name="langgraph" size={20} /><span>{"LangGraph"}</span></div>
          </div>
        </div>
      </div>
    </section>

    <section id="certifications" class="section">
      <h2 class="section-title">{"Certifications"}</h2>
      <div class="certifications-list">
        <div class="certification-item">
          <span>{"Java (Problem Solving) - Intermediate"}</span>
          <span class="issuer">{"HackerRank"}</span>
        </div>
        <div class="certification-item">
          <span>{"JavaScript - Intermediate"}</span>
          <span class="issuer">{"HackerRank"}</span>
        </div>
        <div class="certification-item">
          <span>{"Claude AI Courses"}</span>
          <span class="issuer">{"Anthropic"}</span>
        </div>
      </div>
    </section>

    <section id="contact" class="section">
      <h2 class="section-title">{"Contact"}</h2>
      <div class="contact-list">
        <a href="mailto:ganesh3075@gmail.com" class="contact-link">{"ganesh3075@gmail.com"}</a>
        <a href="tel:+919404018678" class="contact-link">{"+91 94040 18678"}</a>
        <div class="contact-link">{"Pune, Maharashtra"}</div>
      </div>
    </section>
  </div>

  <style>
    .home {
      animation: fadeIn 0.6s ease-in;
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    .section {
      margin-bottom: 1rem;
    }

    .section-title {
      font-size: 1rem;
      font-weight: 700;
      color: var(--text-primary);
      margin: 0 0 1rem 0;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .projects-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 0.75rem;
    }

    .project-card {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 6px;
      padding: 1rem;
      text-decoration: none;
      color: inherit;
      transition: border-color 0.2s;
    }

    .project-card:hover {
      border-color: var(--accent-color);
    }

    .project-title {
      font-size: 0.9375rem;
      font-weight: 600;
      margin: 0 0 0.375rem 0;
      color: var(--text-primary);
    }

    .project-description {
      font-size: 0.8125rem;
      line-height: 1.5;
      margin: 0 0 0.75rem 0;
      color: var(--text-secondary);
    }

    .project-tech {
      display: flex;
      flex-wrap: wrap;
      gap: 0.25rem;
    }

    .tech-tag {
      background: var(--bg-secondary);
      color: var(--accent-color);
      padding: 0.125rem 0.5rem;
      border-radius: 4px;
      font-size: 0.6875rem;
      font-weight: 500;
    }

    .experience-list {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .experience-item {
      padding: 1rem;
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 6px;
    }

    .experience-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.375rem;
    }

    .experience-company {
      font-size: 0.9375rem;
      font-weight: 600;
      margin: 0;
      color: var(--text-primary);
    }

    .experience-period {
      font-size: 0.6875rem;
      font-weight: 500;
      color: var(--accent-color);
    }

    .experience-role {
      font-size: 0.8125rem;
      line-height: 1.5;
      margin: 0;
      color: var(--text-secondary);
    }

    .skills-categories {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .skill-category {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 6px;
      padding: 0.75rem;
    }

    .category-title {
      font-size: 0.6875rem;
      font-weight: 600;
      color: var(--text-secondary);
      margin: 0 0 0.5rem 0;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .tools-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(70px, 1fr));
      gap: 0.5rem;
    }

    .tool-card {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.25rem;
      padding: 0.5rem;
      background: var(--bg-secondary);
      border-radius: 4px;
      color: var(--text-primary);
    }

    .tool-card span {
      font-size: 0.625rem;
      font-weight: 500;
    }

    .certifications-list {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .certification-item {
      padding: 0.75rem;
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 6px;
      display: flex;
      justify-content: space-between;
      font-size: 0.8125rem;
    }

    .certification-item .issuer {
      color: var(--accent-color);
      font-weight: 500;
    }

    .contact-list {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .contact-link {
      padding: 0.75rem;
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 6px;
      font-size: 0.8125rem;
      color: var(--text-secondary);
      text-decoration: none;
    }

    .contact-link:hover {
      border-color: var(--accent-color);
      color: var(--accent-color);
    }
  </style>
}
